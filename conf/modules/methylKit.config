process {
  withName: METHYLKIT {
    publishDir = [
      // expects diffmeth_${genomic_feature}_${generation}_DBP${treatment}.Rds
      path:     { "${params.outdir}/tmp" },
      mode:     params.publish_dir_mode,
      pattern:  '*.Rds'
    ]
  }
}